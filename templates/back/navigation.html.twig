<nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <!-- Container wrapper -->
    <div class="container">
    <!-- Navbar brand -->
        <a class="navbar-brand" href="#">Retour au site</a>

        <!-- Collapsible wrapper -->
        <div class="collapse navbar-collapse">
            <!-- Left links -->
            <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link text-light {% if active_page is defined and active_page == 'profil' %}active{% endif %}" href="{{ path('dashboard') }}">Profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light {% if active_page is defined and active_page == 'project' %}active{% endif %}" href="{{ path('list.project') }}">Projets</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light {% if active_page is defined and active_page == 'skill' %}active{% endif %}" href="{{ path('list.skill') }}">Compétences</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-light {% if active_page is defined and active_page == 'category' %}active{% endif %}" href="{{ path('list.category') }}">Catégories</a>
                </li>
            </ul>
        </div>
    </div>
    <!-- Container wrapper -->
</nav>